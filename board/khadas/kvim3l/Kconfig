if TARGET_KHADAS_VIM3L

config SYS_SOC
	default "g12a"

config SYS_BOARD
	default "kvim3l"

config SYS_VENDOR
	default "khadas"

config SYS_CONFIG_NAME
	default "kvim3l"

config CONFIG_AML_ANTIROLLBACK
	bool
	default n

config DTB_BIND_KERNEL
	bool "load dtb from boot part but not flash rsv"
	default n

endif
